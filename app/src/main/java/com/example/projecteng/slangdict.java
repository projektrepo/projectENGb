package com.example.projecteng;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;


import java.net.URL;

import javax.net.ssl.HttpsURLConnection;

public class slangdict extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_slangdict);
      //  URL urbanapireq = new URL("http://api.urbandictionary.com/v0/define?term={word}");

     //   HttpsURLConnection myConnection = (HttpsURLConnection) urbanapireq.openConnection();

    }
}
